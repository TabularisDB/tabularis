# WEB-073 — Generic exports and logs

**Phase:** Phase 7 — Files, jobs, backup, and import/export parity (12–18 days)

- Migrate query export, AI activity export, logs export, blob files, theme import/export, and diagram export.
- Browser downloads replace native save dialogs.

Acceptance:

- Large-file tests demonstrate bounded memory use.
- Interrupted jobs and expired tokens clean resources.

## Completion record

Before finishing, update the matching row in `web-ui-project/tasks/PROGRESS.md` with `COMPLETED` or `BLOCKED`, a concise summary, verification evidence, and the date. Do not use `|` inside progress fields.
