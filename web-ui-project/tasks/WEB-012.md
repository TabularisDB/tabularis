# WEB-012 — Define platform capabilities

**Phase:** Phase 1 — Frontend transport seam (8–12 days)

- Add the semantic capability interface.
- Implement the Tauri adapter first.
- Add capability negotiation and unsupported-capability errors.
- Centralize environment detection.

Acceptance:

- No migrated component imports dialog, filesystem, opener, clipboard, event, or window APIs directly.

## Completion record

Before finishing, update the matching row in `web-ui-project/tasks/PROGRESS.md` with `COMPLETED` or `BLOCKED`, a concise summary, verification evidence, and the date. Do not use `|` inside progress fields.
