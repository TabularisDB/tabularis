# WEB-021 — Move frontend source and assets

**Phase:** Phase 2 — Create `packages/web-ui` without duplication (5–8 days)

- Move, do not copy, root `src/`, relevant `public/`, and `index.html`.
- Update aliases, test setup, coverage paths, scripts, and imports.
- Update Tauri `frontendDist` and development URL integration.
- Preserve root output compatibility only if release tooling requires it.

Acceptance:

- Exactly one implementation of every component exists.
- Desktop development and release builds use `packages/web-ui`.
- Frontend test count does not decrease unexpectedly.

## Completion record

Before finishing, update the matching row in `web-ui-project/tasks/PROGRESS.md` with `COMPLETED` or `BLOCKED`, a concise summary, verification evidence, and the date. Do not use `|` inside progress fields.
