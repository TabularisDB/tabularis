# WEB-002 — Add architecture decision records

**Phase:** Phase 0 — Governance and measurable baseline (3–5 days)

Document:

- command RPC rather than duplicated REST APIs;
- shared UI package rather than a second UI;
- HTTP + WebSocket transport;
- local-only secure default;
- upload/download tokens instead of browser-controlled server paths;
- updater and desktop-window semantic adaptations.

Acceptance:

- Decisions and rejected alternatives are reviewable before implementation.

## Completion record

Before finishing, update the matching row in `web-ui-project/tasks/PROGRESS.md` with `COMPLETED` or `BLOCKED`, a concise summary, verification evidence, and the date. Do not use `|` inside progress fields.
