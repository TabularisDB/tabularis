# WEB-050 — Connections, groups, tags, and appearance

**Phase:** Phase 5 — Core database parity (15–22 days)

Migrate connection CRUD, duplication, active connections, groups, ordering, tags, driver manifests, connection icons, and connection test progress.

Special handling:

- icon selection uses upload tokens;
- secret values are write-only from the browser;
- responses never echo stored credentials.

## Completion record

Before finishing, update the matching row in `web-ui-project/tasks/PROGRESS.md` with `COMPLETED` or `BLOCKED`, a concise summary, verification evidence, and the date. Do not use `|` inside progress fields.
