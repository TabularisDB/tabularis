# WEB-051 — SSH and K8s connections

**Phase:** Phase 5 — Core database parity (15–22 days)

Migrate SSH profiles, K8s profiles, validation, tunnel lifecycle, progress events, and askpass.

Special handling:

- askpass prompts are bound to the initiating session;
- unattended prompts time out and cancel safely;
- remote mode requires stronger authorization.

## Completion record

Before finishing, update the matching row in `web-ui-project/tasks/PROGRESS.md` with `COMPLETED` or `BLOCKED`, a concise summary, verification evidence, and the date. Do not use `|` inside progress fields.
