# WEB-055 — Database objects and user management

**Phase:** Phase 5 — Core database parity (15–22 days)

Migrate views, materialized views, routines, triggers, DDL generation, users, grants, passwords, and privilege application.

Acceptance for Phase 5:

- Connections, editor, schema explorer, result grid, object editors, and user management pass browser E2E tests against MySQL, PostgreSQL, and SQLite.

## Completion record

Before finishing, update the matching row in `web-ui-project/tasks/PROGRESS.md` with `COMPLETED` or `BLOCKED`, a concise summary, verification evidence, and the date. Do not use `|` inside progress fields.
