# WEB-070 — Upload/download token service

**Phase:** Phase 7 — Files, jobs, backup, and import/export parity (12–18 days)

- Stream files rather than buffering whole files in memory.
- Use opaque tokens, size limits, expiration, ownership, and cleanup.
- Add safe filenames and content types.
- Add tests for traversal, token guessing, reuse, and interrupted transfers.

## Completion record

Before finishing, update the matching row in `web-ui-project/tasks/PROGRESS.md` with `COMPLETED` or `BLOCKED`, a concise summary, verification evidence, and the date. Do not use `|` inside progress fields.
