# WEB-020 — Scaffold workspace package

**Phase:** Phase 2 — Create `packages/web-ui` without duplication (5–8 days)

- Add `packages/web-ui/package.json` with name `@tabularis/web-ui`.
- Move Vite, TypeScript, Vitest, PostCSS, and frontend-specific configuration.
- Add package scripts for dev, build, typecheck, lint, and test.
- Keep root scripts as compatibility delegators.

Acceptance:

```bash
pnpm --filter @tabularis/web-ui dev
pnpm --filter @tabularis/web-ui build
pnpm --filter @tabularis/web-ui test --run
```

all work.

## Completion record

Before finishing, update the matching row in `web-ui-project/tasks/PROGRESS.md` with `COMPLETED` or `BLOCKED`, a concise summary, verification evidence, and the date. Do not use `|` inside progress fields.
