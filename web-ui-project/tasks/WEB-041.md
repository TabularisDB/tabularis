# WEB-041 — Implement WebSocket event bus

**Phase:** Phase 4 — RPC and event infrastructure (8–12 days)

Support:

- connection health;
- query cancellation/status;
- dump/import/export progress;
- plugin installation progress;
- AI approvals/activity;
- SSH askpass;
- update/server lifecycle notifications where applicable.

Add reconnection, heartbeat, subscription cleanup, session scoping, and bounded queues.

Acceptance:

- Slow/disconnected clients cannot grow memory without bounds.
- Events are delivered only to authorized sessions.

## Completion record

Before finishing, update the matching row in `web-ui-project/tasks/PROGRESS.md` with `COMPLETED` or `BLOCKED`, a concise summary, verification evidence, and the date. Do not use `|` inside progress fields.
