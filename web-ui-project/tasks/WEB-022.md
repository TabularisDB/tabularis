# WEB-022 — Make routing server-safe

**Phase:** Phase 2 — Create `packages/web-ui` without duplication (5–8 days)

- Ensure direct navigation to every route receives SPA fallback.
- Verify `/connections`, `/editor`, `/settings`, schema diagrams, visual explain, JSON viewer, task manager, and result sessions.
- Decide BrowserRouter base-path behavior.

Acceptance:

- Refreshing every supported browser route succeeds.

## Completion record

Before finishing, update the matching row in `web-ui-project/tasks/PROGRESS.md` with `COMPLETED` or `BLOCKED`, a concise summary, verification evidence, and the date. Do not use `|` inside progress fields.
