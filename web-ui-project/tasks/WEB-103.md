# WEB-103 — Merge readiness

**Phase:** Phase 10 — Final parity gate (8–12 days)

Required gates:

```bash
pnpm lint
pnpm typecheck
pnpm test:coverage
pnpm build
pnpm test:rust
pnpm --filter @tabularis/web-ui build
```

Also require:

- E2E matrix green;
- security review complete;
- GitNexus change detection reviewed against `main`;
- no unapproved direct Tauri imports in shared UI;
- no duplicate business logic in web handlers;
- release artifacts manually smoke-tested on all supported operating systems.

## Completion record

Before finishing, update the matching row in `web-ui-project/tasks/PROGRESS.md` with `COMPLETED` or `BLOCKED`, a concise summary, verification evidence, and the date. Do not use `|` inside progress fields.
