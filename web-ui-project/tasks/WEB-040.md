# WEB-040 — Implement versioned RPC dispatcher

**Phase:** Phase 4 — RPC and event infrastructure (8–12 days)

- Decode command name and typed payload.
- Map application errors into stable error codes.
- Add request deadlines and cancellation identifiers.
- Add authorization metadata per command group.
- Avoid a second copy of business logic or validation.

Acceptance:

- Representative Phase 1 commands execute over real HTTP.
- Tauri and HTTP contract fixtures produce equivalent results.

## Completion record

Before finishing, update the matching row in `web-ui-project/tasks/PROGRESS.md` with `COMPLETED` or `BLOCKED`, a concise summary, verification evidence, and the date. Do not use `|` inside progress fields.
