# WEB-054 — Data editing and blobs

**Phase:** Phase 5 — Core database parity (15–22 days)

Migrate insert, update, delete, referenced records, MIME detection, blob upload, preview, and download.

Special handling:

- replace server file paths with upload/download tokens;
- enforce payload limits and temporary-file cleanup.

## Completion record

Before finishing, update the matching row in `web-ui-project/tasks/PROGRESS.md` with `COMPLETED` or `BLOCKED`, a concise summary, verification evidence, and the date. Do not use `|` inside progress fields.
