# WEB-053 — Query execution

**Phase:** Phase 5 — Core database parity (15–22 days)

Migrate single and batch execution, count, server time, explain plan, cancellation, active connection registration, and disconnect.

Special handling:

- define response-size policy and optional pagination/streaming;
- cancellation survives WebSocket reconnect through request IDs;
- concurrent browser sessions are isolated.

## Completion record

Before finishing, update the matching row in `web-ui-project/tasks/PROGRESS.md` with `COMPLETED` or `BLOCKED`, a concise summary, verification evidence, and the date. Do not use `|` inside progress fields.
