# WEB-082 — Plugin UI extensions

**Phase:** Phase 8 — AI, plugins, and operational parity (15–22 days)

- Serve installed UI bundles through authenticated endpoints.
- Reuse the existing `@tabularis/plugin-api` host contract.
- Add API-version compatibility checks.
- Apply a strict CSP and document the trust model for third-party JavaScript.
- Verify every existing slot in desktop and web.

## Completion record

Before finishing, update the matching row in `web-ui-project/tasks/PROGRESS.md` with `COMPLETED` or `BLOCKED`, a concise summary, verification evidence, and the date. Do not use `|` inside progress fields.
