# WEB-011 — Add `TabularisClientProvider`

**Phase:** Phase 1 — Frontend transport seam (8–12 days)

- Create one client instance at the application root.
- Expose hooks that do not know the selected transport.
- Add Tauri transport using current `invoke`, `listen`, and `emit` APIs.
- Preserve current behavior.

Acceptance:

- Initial migrated callers pass existing tests.
- Components do not import transport implementations.

## Completion record

Before finishing, update the matching row in `web-ui-project/tasks/PROGRESS.md` with `COMPLETED` or `BLOCKED`, a concise summary, verification evidence, and the date. Do not use `|` inside progress fields.
