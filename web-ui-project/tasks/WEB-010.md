# WEB-010 — Define typed command and event contracts

**Phase:** Phase 1 — Frontend transport seam (8–12 days)

- Add `CommandMap`, `EventMap`, shared error shape, and request IDs.
- Begin with startup, connections, metadata, and query commands.
- Add compile-time contract tests.
- Define a controlled escape hatch for commands not migrated yet; track every use.

Acceptance:

- A typed call catches invalid payloads and return assumptions at compile time.
- No `any` is introduced.

## Completion record

Before finishing, update the matching row in `web-ui-project/tasks/PROGRESS.md` with `COMPLETED` or `BLOCKED`, a concise summary, verification evidence, and the date. Do not use `|` inside progress fields.
