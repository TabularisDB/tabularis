# WEB-060 — Settings, keybindings, and themes

**Phase:** Phase 6 — Persistence and productivity parity (10–15 days)

- Migrate config, prompts, editor preferences, keybindings, custom themes, and connection selection state.
- Separate user/session preferences from server-global settings where concurrent sessions could conflict.

## Completion record

Before finishing, update the matching row in `web-ui-project/tasks/PROGRESS.md` with `COMPLETED` or `BLOCKED`, a concise summary, verification evidence, and the date. Do not use `|` inside progress fields.
