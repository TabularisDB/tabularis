# WEB-094 — Packaging

**Phase:** Phase 9 — Browser adaptations and release hardening (12–18 days)

- Include `packages/web-ui/dist` in Linux, macOS, and Windows artifacts.
- Verify path resolution when launched outside the repository.
- Verify `tabularis --web` on headless Linux without WebKit/display initialization.
- Add container/systemd examples only after the security model is complete.

## Completion record

Before finishing, update the matching row in `web-ui-project/tasks/PROGRESS.md` with `COMPLETED` or `BLOCKED`, a concise summary, verification evidence, and the date. Do not use `|` inside progress fields.
