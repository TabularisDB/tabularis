# WEB-081 — Plugin driver lifecycle

**Phase:** Phase 8 — AI, plugins, and operational parity (15–22 days)

- Reuse the existing plugin manager and JSON-RPC subprocess lifecycle.
- Migrate registry, preview, readme, install, cancel, uninstall, enable, disable, startup errors, and task actions.
- Define which filesystem/plugin-directory commands are local-admin only.

## Completion record

Before finishing, update the matching row in `web-ui-project/tasks/PROGRESS.md` with `COMPLETED` or `BLOCKED`, a concise summary, verification evidence, and the date. Do not use `|` inside progress fields.
