# WEB-042 — Implement browser HTTP transport

**Phase:** Phase 4 — RPC and event infrastructure (8–12 days)

- Add session bootstrap.
- Add typed RPC calls.
- Add WebSocket subscriptions and reconnection.
- Normalize errors to the same frontend error model as Tauri.
- Select transport once during application bootstrap.

Acceptance:

- The Phase 1 vertical slice works in a real browser.

## Completion record

Before finishing, update the matching row in `web-ui-project/tasks/PROGRESS.md` with `COMPLETED` or `BLOCKED`, a concise summary, verification evidence, and the date. Do not use `|` inside progress fields.
