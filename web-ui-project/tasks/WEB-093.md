# WEB-093 — Remote authenticated mode

**Phase:** Phase 9 — Browser adaptations and release hardening (12–18 days)

- Add password/auth proxy mode, TLS/reverse-proxy documentation, rate limiting, audit events, secure cookies, and origin allowlist.
- Add explicit high-risk capability policy.
- Perform threat modeling before enabling non-loopback bind.

## Completion record

Before finishing, update the matching row in `web-ui-project/tasks/PROGRESS.md` with `COMPLETED` or `BLOCKED`, a concise summary, verification evidence, and the date. Do not use `|` inside progress fields.
