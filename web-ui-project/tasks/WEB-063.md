# WEB-063 — Secondary windows

**Phase:** Phase 6 — Persistence and productivity parity (10–15 days)

Replace Tauri window operations with shared session models and routes:

- results window -> `/results-window?session=...`;
- JSON viewer -> `/json-viewer?session=...`;
- schema diagram -> `/schema-diagram?...`;
- visual explain -> `/visual-explain?...`;
- task manager -> `/task-manager`;
- connection window -> route/new browser tab.

Acceptance:

- Browser refresh and new-tab flows preserve or clearly expire session state.
- Desktop continues to use native windows through its capability adapter.

## Completion record

Before finishing, update the matching row in `web-ui-project/tasks/PROGRESS.md` with `COMPLETED` or `BLOCKED`, a concise summary, verification evidence, and the date. Do not use `|` inside progress fields.
