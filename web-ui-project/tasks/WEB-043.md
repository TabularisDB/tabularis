# WEB-043 — Add dual-transport contract harness

**Phase:** Phase 4 — RPC and event infrastructure (8–12 days)

- Define reusable behavior suites.
- Run suites against the Tauri adapter mock and live web test server.
- Add serialization fixtures for complex results, blobs, decimals, dates, and nullable values.

Acceptance:

- Contract drift fails CI.

## Completion record

Before finishing, update the matching row in `web-ui-project/tasks/PROGRESS.md` with `COMPLETED` or `BLOCKED`, a concise summary, verification evidence, and the date. Do not use `|` inside progress fields.
