# WEB-071 — Connection import/export and backup

**Phase:** Phase 7 — Files, jobs, backup, and import/export parity (12–18 days)

- Migrate encrypted connection export/import.
- Migrate foreign-application import using uploaded files.
- Migrate backup status, password setup, manual runs, and generated downloads.
- Clearly distinguish server-host backup targets from browser downloads.

## Completion record

Before finishing, update the matching row in `web-ui-project/tasks/PROGRESS.md` with `COMPLETED` or `BLOCKED`, a concise summary, verification evidence, and the date. Do not use `|` inside progress fields.
