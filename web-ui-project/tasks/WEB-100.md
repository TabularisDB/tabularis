# WEB-100 — Automated E2E matrix

**Phase:** Phase 10 — Final parity gate (8–12 days)

Run browser E2E tests against real:

- SQLite;
- PostgreSQL;
- MySQL;
- optional SSH and K8s fixtures;
- one driver plugin;
- one UI extension plugin.

Cover Chromium first; add Firefox and WebKit where browser APIs allow equivalent behavior.

## Completion record

Before finishing, update the matching row in `web-ui-project/tasks/PROGRESS.md` with `COMPLETED` or `BLOCKED`, a concise summary, verification evidence, and the date. Do not use `|` inside progress fields.
