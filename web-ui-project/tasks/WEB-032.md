# WEB-032 — Add HTTP server skeleton

**Phase:** Phase 3 — Headless backend bootstrap (8–12 days)

- Add Axum or the selected Rust web framework.
- Serve health, session negotiation, static assets, and SPA fallback.
- Embed release assets or resolve them relative to the installation reliably.
- Implement graceful shutdown and cleanup of tunnels/plugins/jobs.
- Print the final URL and optionally open the browser.

Acceptance:

- Packaged `tabularis --web --no-open` starts without a graphical session.
- `curl /healthz` succeeds.
- Browser loads the shared UI.

## Completion record

Before finishing, update the matching row in `web-ui-project/tasks/PROGRESS.md` with `COMPLETED` or `BLOCKED`, a concise summary, verification evidence, and the date. Do not use `|` inside progress fields.
