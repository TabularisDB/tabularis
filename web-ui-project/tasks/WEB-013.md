# WEB-013 — Migrate a representative vertical slice

**Phase:** Phase 1 — Frontend transport seam (8–12 days)

Migrate:

- `is_debug_mode`;
- `get_connections`;
- `test_connection`;
- `get_tables`;
- `execute_query`;
- `cancel_query` and query events.

Acceptance:

- Desktop functionality is unchanged.
- The slice proves request, response, errors, cancellation, and events.

## Completion record

Before finishing, update the matching row in `web-ui-project/tasks/PROGRESS.md` with `COMPLETED` or `BLOCKED`, a concise summary, verification evidence, and the date. Do not use `|` inside progress fields.
