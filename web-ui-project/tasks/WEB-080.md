# WEB-080 — AI and approval flows

**Phase:** Phase 8 — AI, plugins, and operational parity (15–22 days)

- Migrate key status/write/delete, model discovery, generation, explanation, plan analysis, naming, activity, sessions, exports, and approvals.
- Never send provider keys back to the browser.
- Scope pending approval prompts to authorized sessions.

## Completion record

Before finishing, update the matching row in `web-ui-project/tasks/PROGRESS.md` with `COMPLETED` or `BLOCKED`, a concise summary, verification evidence, and the date. Do not use `|` inside progress fields.
