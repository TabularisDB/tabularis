# WEB-014 — Enforce frontend boundaries

**Phase:** Phase 1 — Frontend transport seam (8–12 days)

- Add ESLint restrictions preventing Tauri imports outside adapter files.
- Add a CI check for direct `invoke()` use.
- Maintain an explicit temporary allowlist and drive it to zero.

Acceptance:

- New direct Tauri coupling fails CI.

## Completion record

Before finishing, update the matching row in `web-ui-project/tasks/PROGRESS.md` with `COMPLETED` or `BLOCKED`, a concise summary, verification evidence, and the date. Do not use `|` inside progress fields.
