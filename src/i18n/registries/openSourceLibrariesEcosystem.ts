import { msg } from "@lingui/core/macro";
import type { MessageDescriptor } from "@lingui/core";

export const openSourceLibrariesEcosystem: Record<string, MessageDescriptor> = {
  npm: msg`npm ecosystem`,
  cargo: msg`Cargo ecosystem`,
};
